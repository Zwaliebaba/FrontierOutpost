// FrontierOutpost/src/liblt/Shaders/Ui.hlsli
//
// GameData/shader/common/ui.jsl, in HLSL. ui.jsl's width and height varyings, which no vertex shader
// wrote and no shader read, are left out.

#ifndef UI_HLSLI
#define UI_HLSLI

#include "Common.hlsli"
#include "Math.hlsli"
#include "Noise.hlsli"

float2 frame;
float2 size;
float hover;
float opacity;

#endif
